# 采用独立 Lua 滤镜实现成语动态注入

四字成语的渐进式双轨输入不通过 `speller/algebra` 原生派生，而是通过独立的 `lua/9jianhu_chengyu.lua` 滤镜实现。

若使用原生代数派生，主词典中 147 万条海量词汇均会被派生为 4 击，不仅会导致 `9jianhu.prism.bin` 体积剧烈膨胀（>20MB），还会严重污染 4 击首屏候选。采用 Lua 内存倒排索引将成语范围限制在精选高频成语表内，兼具纯净命中率与零 Prism 膨胀。
